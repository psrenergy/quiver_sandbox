name: quiver_sandbox
description: A Dart library for running Deno scripts in a permission-scoped sandbox.
version: 1.0.0
repository: https://github.com/psrenergy/quiver_sandbox

environment:
  sdk: ^3.11.1

dependencies:
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
