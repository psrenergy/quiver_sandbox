name: quiver_sandbox
description: Internal sandbox runner for Deno scripts. Extracted from the host app for test isolation; not published.
version: 1.0.0
publish_to: none
repository: https://github.com/psrenergy/quiver_sandbox

environment:
  sdk: ^3.11.1

dependencies:
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
